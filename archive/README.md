# Archive

Historical material kept so we do not lose it, but it is not the source of truth.

| Path | Contents |
|---|---|
| `legacy_nlse/` | The old monolithic `nlse.py` |
| `scripts/` | Duplicate XPM trainer and non-batch ring scripts |
| `notebooks/` | Exploratory / superseded notebooks |
| `strawberry_fields/` | Separate cubic-phase / squeezing work |
| `docs/` | Primer PDF and notes |
| `vis/` | Animation videos (if present) |
| `results_plots/` | One-off ring plot dumps |
| `results_full/` | Frozen `res/` experiment trees. Nested `wandb/` logs are gitignored |

Prefer the packages under `nlse/` and `approaches/`.
